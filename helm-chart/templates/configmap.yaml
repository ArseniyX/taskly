apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "taskly.fullname" . }}-config
  labels:
    {{- include "taskly.labels" . | nindent 4 }}
data:
  next-public-domain: {{ .Values.nextPublicDomain | quote }}